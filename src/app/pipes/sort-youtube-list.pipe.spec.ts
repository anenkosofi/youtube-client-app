import { SortYoutubeListPipe } from './sort-youtube-list.pipe';

describe('SortYoutubeListPipe', () => {
  it('create an instance', () => {
    const pipe = new SortYoutubeListPipe();
    expect(pipe).toBeTruthy();
  });
});
